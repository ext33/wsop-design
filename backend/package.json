{
  "name": "backend",
  "version": "1.0.0",
  "main": "dist/index.js",
  "private": "true",
  "scripts": {
    "start": "tsc && node dist/index.js",
    "dev": "nodemon --watch './*' --ext 'ts,json' --ignore 'src/**/*.spec.ts' --exec 'tsc && node dist/index.js' dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/express": "^4.16.1",
    "nodemon": "^2.0.7",
    "tslint": "^5.12.1",
    "typescript": "^3.3.3"
  },
  "dependencies": {
    "argon2": "^0.27.1",
    "cors": "^2.8.5",
    "express": "^4.16.4",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.12.0",
    "multer": "^1.4.2",
    "node-schedule": "^2.0.0"
  }
}
