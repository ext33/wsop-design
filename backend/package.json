{
  "name": "backend",
  "version": "1.0.0",
  "main": "dist/index.js",
  "private": "true",
  "scripts": {
    "start": "tsc && node dist/index.js",
    "dev": "tsc && nodemon --watch './*' --ext 'ts,json' --ignore 'src/**/*.spec.ts' --exec 'tsc && node dist/index.js' dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/express": "^4.16.1",
    "tslint": "^5.12.1",
    "typescript": "^3.3.3",
    "nodemon": "^2.0.7"
  },
  "dependencies": {
    "express": "^4.16.4",
    "mongoose": "^5.12.0",
    "body-parser": "^1.19.0",
    "multer": "^1.4.2",
    "cors": "^2.8.5"
  }
}
